/**
 * A fixed, accessible color palette for categories (GitHub-label-style). Kept as a closed set of
 * keys — rather than freeform hex — so every color stays legible and theme-aware in both light
 * and dark mode.
 */
export enum CATEGORY_COLOR {
	RED = 'red',
	ORANGE = 'orange',
	AMBER = 'amber',
	YELLOW = 'yellow',
	LIME = 'lime',
	GREEN = 'green',
	TEAL = 'teal',
	CYAN = 'cyan',
	BLUE = 'blue',
	INDIGO = 'indigo',
	VIOLET = 'violet',
	PINK = 'pink',
	GRAY = 'gray',
}
